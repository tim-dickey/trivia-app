# Development Dockerfile for React frontend
# Includes hot reload via Vite

FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files first for better caching
COPY package*.json ./

# Install dependencies
# Using npm install since package-lock.json may not exist
RUN npm install

# Copy application code
# Note: In docker-compose, we'll mount the code as a volume for hot reload
COPY . .

# Expose port
EXPOSE 5173

# Default command (can be overridden in docker-compose)
# --host flag allows access from outside the container
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
